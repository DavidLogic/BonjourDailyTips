<?xml version="1.0" encoding="utf-8" ?>
<ContentView
    x:Class="BonjourDailyTips.Controls.DayCounterControl"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:matcl="clr-namespace:Material.Components.Maui;assembly=Material.Components.Maui">
    <Border Margin="8,8,8,8" Padding="26" Background="{DynamicResource WhiteBrush}" >
        <Border.StrokeShape>
            <RoundRectangle
                BackgroundColor="Transparent"
                CornerRadius="8"
                StrokeThickness="0" />
        </Border.StrokeShape>
        <Grid>
            <Grid ColumnDefinitions="auto">
                <Label Text="Logic生日" />

            </Grid>
        </Grid>
    </Border>
</ContentView>
